const telemetryArea = document.querySelector("#header-telemetry")

/* window.onload = function() {
    console.log(telemetryArea.childNodes.length);
    if(telemetryArea.childNodes.length === 0){
        telemetryArea.remove();
    }
} */